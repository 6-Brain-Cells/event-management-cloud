apiVersion: v1
kind: Secret
metadata:
  name: event-mgmt-secrets
  namespace: {{ .Values.global.namespace }}
type: Opaque
stringData:
{{- range $key, $val := .Values.global.secrets }}
  {{ $key }}: {{ $val | quote }}
{{- end }}
